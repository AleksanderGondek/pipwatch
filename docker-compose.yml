version: '3'
services:
    pipwatch_api:
        build: ./api
        ports:
            - "8081:8081"
    pipwatch_worker:
        build: ./worker
    redis:
        image: "redis:latest"
        ports:
            - "6379:6379"
